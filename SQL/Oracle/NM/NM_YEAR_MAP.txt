CREATE TABLE NM_YEAR_MAP
(
  YEAR_MAP_ID       VARCHAR2(16 BYTE),
  BATCH_MAP_ID      VARCHAR2(16 BYTE),
  YEAR_ID           VARCHAR2(16 BYTE),
  YEAR_NUMBER       NUMBER,
  YEAR_NAME         VARCHAR2(100 BYTE)          NOT NULL,
  ENTERED_BY        VARCHAR2(16 BYTE),
  ENTRY_TIMESTAMP   DATE,
  UPDATED_BY        VARCHAR2(16 BYTE),
  UPDATE_TIMESTAMP  DATE,
  COMPANY_NO        VARCHAR2(10 BYTE)           DEFAULT 1
);


ALTER TABLE NM_YEAR_MAP ADD (
  CONSTRAINT YEAR_MAP_ID_PK
  PRIMARY KEY  (YEAR_MAP_ID));

ALTER TABLE NM_YEAR_MAP ADD (
  CONSTRAINT BATCH_MAP_ID_FK 
  FOREIGN KEY (BATCH_MAP_ID) 
  REFERENCES NM_BATCH_MAP (BATCH_MAP_ID));
