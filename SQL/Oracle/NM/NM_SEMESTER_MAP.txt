CREATE TABLE NM_SEMESTER_MAP
(
  SEMESTER_MAP_ID   VARCHAR2(16 BYTE),
  YEAR_MAP_ID       VARCHAR2(16 BYTE),
  SEMESTER_ID       VARCHAR2(16 BYTE),
  SEMESTER_NUMBER   NUMBER,
  SEMESTER_NAME     VARCHAR2(100 BYTE)          NOT NULL,
  ENTERED_BY        VARCHAR2(16 BYTE),
  ENTRY_TIMESTAMP   DATE,
  UPDATED_BY        VARCHAR2(16 BYTE),
  UPDATE_TIMESTAMP  DATE,
  COMPANY_NO        VARCHAR2(10 BYTE)           DEFAULT 1
);


ALTER TABLE NM_SEMESTER_MAP ADD (
  CONSTRAINT SEMESTER_MAP_ID_PK
  PRIMARY KEY  (SEMESTER_MAP_ID));

ALTER TABLE NM_SEMESTER_MAP ADD (
  CONSTRAINT YEAR_MAP_ID_FK 
  FOREIGN KEY (YEAR_MAP_ID) 
  REFERENCES NM_YEAR_MAP (YEAR_MAP_ID));
